# Generated by `npm install` (run once after `cargo generate` to populate).
# Until then, CI workflows that use `npm ci` will fail.
node_modules/
out/
bin/*.exe
bin/msbuild-diagnostic-mcp
bin/VERSION
*.vsix
